name: Update October ISO version on the website

on:
  release:
    type: [published]

jobs:
  dispatch:
    runs-on: ubuntu-latest
    permissions:
      contents: write
    steps:
      - name: Dispatch website ISO update
        uses: peter-evans/repository-dispatch@v4
        with:
          token: ${{ secrets.WEBSITE_DISPATCH_EVENT_TOKEN }}
          event-type: iso-updated
